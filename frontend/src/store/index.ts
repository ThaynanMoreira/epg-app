import Vue from 'vue';
import Vuex from 'vuex';

import programmes from './epg.module';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    programmes,
  },
});
